# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.env.*
!.env.example
!.env.prod.example

# Node / Mini App
# NOTE: miniapp/dist is intentionally tracked for now — docker-compose mounts it
# into Caddy, and the VPS doesn't yet build it from npm (tracked in open-decisions).
# Once `npm run build` runs on the VPS, untrack it here.
node_modules/
miniapp/.vite/
*.local

# Databases
*.sqlite
*.sqlite3
*.db

# Editor / OS
.vscode/
.idea/
.DS_Store

# Coverage / test artifacts
.pytest_cache/
.coverage
htmlcov/
playwright-report/
