# Bundle F — Settings screen Plan
> REQUIRED SUB-SKILL: superpowers:subagent-driven-development.

Goal: Settings screen (display name edit, pair info, unpair-via-bot). Backend PATCH/GET /api/me.

## Task 1: Backend /api/me (GET + PATCH)
Files: app.py, schemas.py, tests.
- GET /api/me → {id, displayName, tgUsername, pairCreatedAt, partnerDisplayName}. (partner name: query the pair's other member's display_name.)
- PATCH /api/me {displayName?} → validate (truncate_graphemes to 128, non-empty after trim) → update User.display_name → return MeOut.
- Tests: GET returns profile; PATCH updates; empty rejected; over-length truncated.
- Commit "feat(api): GET/PATCH /api/me profile".

## Task 2: Frontend Settings screen
Files: new Settings.tsx, App.tsx (entry), copy.ts, Home.tsx (⚙️ button), test.
- ScreenHeader ⚙️; You section (display name field + Сохранить → PATCH, saved toast); Pair section (togetherSince from stats); Danger section (Расстворить пару → confirm modal → shows "через бота: /unpair" + copy button, no API).
- Entry: ⚙️ in Home ScreenHeader area OR MoreSheet. Pick MoreSheet "Настройки" entry (cleaner).
- copy keys per spec.
- Test: renders; save calls PATCH; unpair confirm shows bot instruction.
- Commit "feat(settings): profile + pair-info + unpair screen".

## Task 3: build, test, deploy
